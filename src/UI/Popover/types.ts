export enum PopoverSide {
    LEFT = 'left',
    RIGHT = 'right',
    TOP = 'top',
    BOTTOM = 'bottom',
}

export enum PopoverPosition {
    START = 'start',
    CENTER = 'center',
    END = 'end',
}
